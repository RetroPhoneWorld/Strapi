'use strict';

/**
 * world-record-page service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::world-record-page.world-record-page');
