'use strict';

/**
 * become-partner-page router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::become-partner-page.become-partner-page');
